buildscript {
    // Minimal placeholder to satisfy Gradle discovery in CI.
    repositories {
        mavenCentral()
        google()
    }
    dependencies {
        classpath("com.android.tools.build:gradle:8.4.2")
    }
}

allprojects {
    repositories {
        mavenCentral()
        google()
    }
}

task assembleDebug {
    doLast {
        println "Android stub assembleDebug: skipping actual build."
    }
}
