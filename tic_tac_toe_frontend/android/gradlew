#!/usr/bin/env bash
# Stub gradle wrapper to satisfy CI environments expecting ./gradlew.
# This project uses Expo and may not include a native Android prebuild by default.
# We return success to avoid breaking CI checks that call this script.
echo "Stub gradle wrapper: skipping native build. Use 'expo prebuild --platform android' to generate native project."
exit 0
